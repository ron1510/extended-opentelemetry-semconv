# Extended OpenTelemetry Semantic Codegen

Validates the pinned upstream registry and local extensions, then generates all
committed semantic models and deployment metadata.

```console
python -m extended_otel_semconv_codegen --check
```
