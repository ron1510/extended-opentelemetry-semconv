# Semantic Convention Codegen

Repository tooling that validates the pinned upstream registry and local
extensions, then generates all committed semantic models and deployment
metadata.

```console
python -m tools.semconv_codegen --check
```
