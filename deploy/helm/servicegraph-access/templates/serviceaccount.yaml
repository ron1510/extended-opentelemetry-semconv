{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "servicegraph-access.serviceAccountName" . }}
  labels:
    {{- include "servicegraph-access.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
